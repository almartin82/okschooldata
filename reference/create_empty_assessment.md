# Create empty assessment data frame

Returns an empty data frame with the expected assessment schema.

## Usage

``` r
create_empty_assessment()
```

## Value

Empty data frame with assessment columns
