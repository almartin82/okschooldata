# Create empty tidy assessment data frame

Returns an empty data frame with the expected tidy assessment schema.

## Usage

``` r
create_empty_tidy_assessment()
```

## Value

Empty data frame with tidy assessment columns
