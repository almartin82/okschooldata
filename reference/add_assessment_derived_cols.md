# Add derived columns to assessment data

Adds aggregation flags and other derived columns.

## Usage

``` r
add_assessment_derived_cols(df, end_year)
```

## Arguments

- df:

  Data frame with standardized columns

- end_year:

  School year end

## Value

Data frame with derived columns
