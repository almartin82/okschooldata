# Get column mappings for OSDE enrollment data

Returns a list mapping OSDE column names to standardized names. OSDE
uses fairly consistent naming but may vary slightly by year.

## Usage

``` r
get_osde_column_map()
```

## Value

Named list of column mappings
